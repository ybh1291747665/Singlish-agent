# 项目记录

本文档记录本项目开发过程中做出的重大决策、决策原因、遇到的问题，以及对应解决方法。

## 2026-06-10 到 2026-06-11 已完成决策

### 决策 1：采用 balanced monorepo 结构

- 决策内容：采用 `apps/api`、`apps/web`、`packages/contracts`、`packages/config`、`infra/docker` 的 monorepo 结构。
- 原因：
  - 需要同时承载后端 API、前端 demo、共享类型以及本地基础设施。
  - 比纯双应用仓库更利于后续共享 contract。
  - 比更重的 workspace 结构更轻，适合当前 demo-first 阶段。

### 决策 2：本地开发时只将基础设施放进 Docker

- 决策内容：PostgreSQL、Redis、MinIO 用 Docker Compose 运行；API、worker、web 直接本机运行。
- 原因：
  - 本机直接跑代码，调试更快。
  - 基础设施更适合容器化，便于统一版本和配置。
  - Windows 环境下如果把全部应用都放入容器，会显著增加开发和排查成本。

### 决策 3：优先完成“可运行的纵向切片”

- 决策内容：先完成健康检查、创建 job、读取 job、假处理 worker、前端上传和轮询，而不是提前实现 ASR 或 Agent 能力。
- 原因：
  - 先证明系统骨架可运行，比先堆空目录或未来能力更有价值。
  - 能更早暴露数据库、队列、对象存储、前后端契约方面的问题。

### 决策 4：测试在无本地基础设施时允许使用最小替身

- 决策内容：在本机未运行 PostgreSQL / Redis / MinIO 时，Task 6 和 Task 7 的部分测试使用 fake session / fake storage / fake queue。
- 原因：
  - 当时环境里没有 Docker 可用，无法进行真实依赖联调。
  - 仍需保证计划中的 API 和 worker 切片可以被验证。
  - 限制替身只用于测试层，生产代码保持真实边界。

### 决策 5：保持应用默认启动不强依赖数据库初始化

- 决策内容：通过 `init_db_on_startup` 控制初始化，默认值为 `False`。
- 原因：
  - 避免 API 在没有 PostgreSQL 时完全无法启动或无法跑测试。
  - 保持健康检查和非 DB 依赖测试可以独立运行。

### 决策 6：将对象存储与队列边界接入真实实现

- 决策内容：
  - `jobs` 路由从 noop storage / noop queue 切换为 `ObjectStorageService` 和 Celery 队列。
  - worker 使用 Celery 任务 `singlish_agent.process_job` 驱动假处理流程。
- 原因：
  - 这一步是从“纯 demo stub”走向“真实系统骨架”的关键。
  - 后续真实音频处理可以继续沿这两个边界演进，不需要重写调用链。

### 决策 7：前端使用最小 Vite + React + Vitest 工作区

- 决策内容：建立 `apps/web` workspace，并创建 `packages/contracts` 共享前后端 TypeScript 类型。
- 原因：
  - 快速搭建可运行 demo UI。
  - 将健康响应、job 创建响应、job 详情响应统一收敛到 contracts。

## 已遇到的问题与解决方法

### 问题 1：最初环境中 Docker 不可用

- 表现：
  - 早期无法运行 `docker compose`。
  - 无法进行真实基础设施联调。
- 解决方法：
  - 先按计划完成代码骨架和测试。
  - 用 fake session / fake infra 完成受限环境下的验证。
  - 待用户安装 Docker 后，再继续进行真实联调。

### 问题 2：API 默认启动时会尝试初始化数据库，导致无 PostgreSQL 时启动失败

- 表现：
  - FastAPI lifespan 中直接执行 `init_db()`。
- 解决方法：
  - 加入 `init_db_on_startup` 配置。
  - 默认不初始化数据库，只在显式开启时执行。

### 问题 3：健康检查中的对象存储探测延迟过高

- 表现：
  - S3 `head_bucket` 在依赖不可达时耗时很长。
- 解决方法：
  - 使用较短的 boto 超时配置。
  - 将阻塞型 S3 调用放入线程，避免卡住事件循环。

### 问题 4：真实 worker 启动时没有注册 `process_job` 任务

- 表现：
  - Celery worker 入口只加载了 `celery_app`，未显式包含任务模块。
  - 真实 worker 运行时可能看不到 `singlish_agent.process_job`。
- 解决方法：
  - 在 Celery app 中加入 `include=["singlish_agent_api.worker.tasks"]`。
  - 新增测试验证任务已经注册到 `celery_app.tasks`。

### 问题 5：job 状态在外部副作用成功前被提前写成成功态

- 表现：
  - 上传失败时 job 可能已被写成 `uploaded`。
  - 入队失败时 job 可能已被写成 `queued`。
- 解决方法：
  - 调整顺序：
    - 上传成功后再转到 `uploaded`
    - 队列发布成功后再转到 `queued`
  - 为上传失败和入队失败分别补测试。

### 问题 6：worker 出错后 job 会卡在 `processing`

- 表现：
  - 假处理任务执行中发生异常时，job 不会进入终态。
- 解决方法：
  - 在 worker 中捕获处理阶段异常。
  - 当状态已进入 `processing` 时，将 job 迁移到 `failed` 后再抛出异常。
  - 增加失败路径测试。

### 问题 7：前端构建因测试回调类型不匹配而失败

- 表现：
  - `UploadPanel.test.tsx` 中的 `onUpload` mock 返回了 `Promise<number>`，与组件要求的 `Promise<void>` 不匹配。
- 解决方法：
  - 改成显式返回 `void` 的异步函数。
  - 重新运行前端测试和构建，确认通过。

## 2026-06-11 当前联调记录

### 决策 8：现在开始进行真实 Docker 基础设施联调

- 决策内容：在确认本机 Docker 已可用后，开始执行 `docker compose` 校验和联调。
- 原因：
  - 此前所有主要计划任务已经完成。
  - 现在最有价值的是验证真实 PostgreSQL / Redis / MinIO 与本地 API / worker / web 的组合是否能跑通。

### 当前状态

- `docker --version` 已确认可用：`29.5.3`
- `docker compose -f infra/docker/docker-compose.yml config` 已通过
- 已完成：
  - Docker Desktop 已启动
  - `docker compose -f infra/docker/docker-compose.yml up -d` 已完成
  - PostgreSQL、Redis、MinIO 已成功运行
  - bucket 初始化容器已 `Exited (0)`
  - `/health` 返回 `app/database/redis/object_storage` 全部 `ok`
  - 真实上传链路已验证成功
    - job 创建成功
    - 文件进入 MinIO：`raw/89a076ea-28e4-4e3e-9f14-01a7d9dde5d4/sample.wav`
    - worker 处理完成
    - 数据库中最新 job 状态为 `completed`

### 决策 9：真实联调时显式执行数据库建表

- 决策内容：在启动 API 和 worker 前，手动执行 `init_db()` 初始化表结构。
- 原因：
  - 当前项目为了测试友好，默认关闭了启动自动建表。
  - 如果直接进入真实上传流程，`jobs` 表可能尚未存在，导致运行时报错。
  - 在没有迁移系统的当前阶段，显式建表是最简单直接的做法。

## 2026-06-11 联调阶段新增问题与解决方法

### 问题 8：Docker CLI 可用，但 daemon 未运行

- 表现：
  - `docker --version` 正常。
  - `docker compose up` 报错：无法连接 `//./pipe/docker_engine`。
- 解决方法：
  - 启动 Docker Desktop。
  - 等待 `docker info` 可正常返回后，再执行 `docker compose up -d`。

### 问题 9：并行执行 `docker compose up` 与 `docker compose ps` 时，`ps` 输出为空

- 表现：
  - 首次拉镜像耗时较长。
  - 与之并行执行的 `ps` 在容器真正创建前就返回了空列表。
- 解决方法：
  - 在 `up -d` 完成后单独再次执行 `docker compose ps`。
  - 使用 `docker ps` 辅助核对健康状态与端口映射。

### 问题 10：PowerShell 的 `Invoke-RestMethod` 不支持 `-Form`

- 表现：
  - 使用 `Invoke-RestMethod -Form` 上传文件时报参数不存在。
- 解决方法：
  - 改用 `curl.exe -F` 发送 multipart 表单。
  - 继续验证真实的 `/api/v1/jobs` 上传链路。

### 问题 11：Windows 下 `Start-Process` 不能直接用 `npm`

- 表现：
  - 启动 Vite 开发服务器时，`Start-Process -FilePath npm` 报 `%1 is not a valid Win32 application`。
- 解决方法：
  - 改用 `C:\Program Files\nodejs\npm.cmd` 作为可执行文件。
  - 随后成功启动前端开发服务器，并确认 `http://127.0.0.1:5173` 返回 `200`。

## 当前结论

- 真实基础设施联调已成功：
  - Docker Compose 基础设施可启动
  - API 可连接 PostgreSQL / Redis / MinIO
  - 文件上传、job 入库、worker 处理、状态完成全部已跑通
- 当前项目已经不只是“测试通过”，而是“在真实本地依赖下也能跑通第一条 demo 链路”

## 2026-06-11 Git 留档决策

### 决策 10：先将项目文档与环境文件纳入版本库，再推送远程

- 决策内容：
  - 先提交设计文档、实现计划、环境文件、依赖文件、项目总计划和 `记录.md`
  - 再等待远程仓库地址，执行真正的 `push`
- 原因：
  - 当前仓库没有配置 `remote`
  - 先把本地提交整理干净，更利于后续一次性推送留档

### 决策 11：将本地 agent 辅助文件排除出源码仓库

- 决策内容：把 `.agents/` 和 `skills-lock.json` 加入 `.gitignore`
- 原因：
  - 这两类内容更偏本地工具链与 agent 辅助资源
  - 不属于当前项目运行所必需的源码与文档
  - 避免污染后续提交历史

### 决策 12：补充独立的启动/停止操作手册

- 决策内容：新增 `docs/启动停止手册.md`
- 原因：
  - README 当前适合快速上手，但不够覆盖启动顺序、停止方式和常见问题
  - 真实联调已经跑通，应该把可重复的操作沉淀下来

## 2026-06-11 下一阶段 A 决策

### 决策 13：先扩展为完整离线处理阶段骨架，不直接接入真实 ASR

- 决策内容：
  - 将当前 worker 的假处理链从 `processing` 扩展为：
    - `preprocessing`
    - `transcribing`
    - `normalizing`
    - `generating_report`
    - `completed`
  - 同步更新后端状态机、worker 流程、前端阶段展示和测试
- 原因：
  - 这是最接近 `project-plan.md` 的下一步演进
  - 先把系统骨架与状态流转补齐，比立刻接真实 ASR 更稳
  - 真实 ASR、真实标准化、真实报告生成后续都可以挂接在这些稳定阶段上

### 决策 14：前后端共享同一套 job 状态字面量

- 决策内容：
  - 在 `packages/contracts` 中加入统一的 pipeline stage 常量与 `JobStatus` 类型
  - 前端阶段组件直接基于共享常量渲染
- 原因：
  - 避免前后端各自维护一套状态字符串
  - 降低状态名变更时的联动成本

## 2026-06-11 下一阶段 A 问题与解决方法

### 问题 12：旧 worker 失败测试仍然绑定旧的 `processing` / `normalizing` 假设

- 表现：
  - 当 worker 阶段扩展后，原失败测试的触发点不再正确
  - 导致测试没有真正打到新的失败路径
- 解决方法：
  - 将失败注入点调整到 `generating_report` 阶段
  - 让测试继续覆盖“处理中失败后转为 `failed`”这一真实意图

### 决策 15：用单个结构化结果载荷承载各阶段产物

- 决策内容：
  - 在 job 上增加 `result_payload`
  - 由一个结构化对象统一保存各阶段假结果：
    - `preprocessing`
    - `transcription`
    - `normalization`
    - `report`
- 原因：
  - 比为每个阶段单独加多列更轻
  - 更适合当前快速演进阶段
  - 后续接真实 ASR / 标准化 / 报告时，只需替换阶段生成逻辑，不必重塑 API 形状

### 决策 16：在没有完整迁移系统前，采用兼容式建列

- 决策内容：
  - 在 `init_db()` 中加入 `ALTER TABLE jobs ADD COLUMN IF NOT EXISTS result_payload TEXT`
- 原因：
  - 当前项目还没有 Alembic 迁移流
  - 仅靠 `create_all()` 无法给已存在的 `jobs` 表补新列
  - 兼容式建列能让现有本地联调环境继续工作，不必手动删库重建

### 问题 13：状态骨架完成后，系统仍然缺少可被前端消费的阶段产物

- 表现：
  - 虽然 worker 已经能走完整阶段链
  - 但 API 仍然只返回 `result_summary`
  - 前端无法展示 transcript、standard English、summary 等更真实内容
- 解决方法：
  - 增加 `result_payload`
  - worker 在每个阶段填充对应的假结果
  - API 返回解析后的结构化对象
  - 前端新增结果详情组件展示这些中间与最终产物

### 问题 14：现有 PostgreSQL 表结构无法自动获得新字段

- 表现：
  - 数据模型增加 `result_payload` 后，已存在的本地 `jobs` 表不会自动更新
- 解决方法：
  - 显式运行 `init_db()`
  - 由 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...` 自动补齐新列
  - 重启 API 与 worker 后再进行真实联调验证

## 2026-06-11 下一阶段 A 真实 ASR 接入决策

### 决策 17：先接入可懒加载的真实 ASR provider，并保留自动回退

- 决策内容：
  - 新增 `infrastructure/asr/provider.py`
  - 将转写能力抽象成 provider
  - 默认使用 `auto` 策略：优先尝试 `faster_whisper`，失败时自动回退到 `fake`
- 原因：
  - 这样可以在不破坏现有 demo 稳定性的前提下，逐步把假链路替换成真链路
  - 本地不同环境的依赖完整度不一致，自动回退能减少“因为缺包导致整条任务失败”的摩擦

### 决策 18：真实音频仍沿用对象存储入口，由 worker 下载后落临时文件再转写

- 决策内容：
  - worker 从 MinIO 下载上传音频
  - 写入临时文件后交给 ASR provider 处理
  - 处理结束后删除临时文件
- 原因：
  - `faster-whisper` 对文件路径输入最直接
  - 这条链路更贴近未来真实处理流程，而不是只在内存里拼演示数据

### 问题 15：基础 Python 环境缺少真实 ASR 依赖

- 表现：
  - 当前正在运行的基础 Python 环境没有 `faster_whisper`、`torch`、`av` 等包
  - 直接在这个环境里启动 worker 时，只能走回退逻辑，无法真正做离线 ASR
- 解决方法：
  - 确认 `singlish-agent` conda 环境中已经安装这些依赖
  - 在该环境里执行 `python -m pip install -e apps/api`
  - 后续将 worker 切换到 `singlish-agent` 环境运行

### 问题 16：结果摘要仍写死为 fake 文案，会误导联调判断

- 表现：
  - 即使真实 provider 生效，`result_summary` 依然显示 fake 完成信息
- 解决方法：
  - 改为按 `transcription.provider` 动态生成摘要
  - 这样 API 与前端都能更清楚地区分当前到底命中了哪条链路

### 问题 17：真实 provider 本身可用，但运行中的 worker 仍可能停留在错误环境

- 表现：
  - 直接验证 provider 时，`singlish-agent` 环境已经可以产出 `faster_whisper` 结果
  - 但如果后台 worker 仍由基础 Python 环境启动，联调时仍只会看到回退效果
- 解决方法：
  - 将 API 与 worker 都切换到 `singlish-agent` conda 环境启动
  - 用真实语音文件重新上传联调，确认任务返回 `result_payload.transcription.provider=faster_whisper`

### 联调记录：真实离线 ASR 已首次跑通

- 验证方式：
  - 通过 PowerShell `System.Speech.Synthesis.SpeechSynthesizer` 生成本地 wav
  - 上传到 `/api/v1/jobs`
  - 轮询任务详情直到完成
- 验证结果：
  - job_id：`8777db1b-c1d2-47af-9c14-87d924e0a29c`
  - `status=completed`
  - `result_summary=Transcription completed successfully via faster_whisper.`
  - `result_payload.transcription.provider=faster_whisper`
  - transcript 为 `This cue is quite fast.`
- 结论：
  - 当前系统已经不只是“假处理链路可跑通”
  - 而是已经具备“真实上传音频 -> worker 下载 -> faster-whisper 转写 -> API 返回结构化结果”的第一条离线真链路

## 2026-06-11 下一阶段 A 导出能力决策

### 决策 19：优先补导出接口，而不是继续堆假阶段细节

- 决策内容：
  - 先实现 `/api/v1/jobs/{job_id}/exports/{format}`
  - 优先打通 Markdown、SRT，并顺带补齐 TXT、JSON、WebVTT
- 原因：
  - `project-plan.md` 第一阶段完成条件明确要求能下载 Markdown 和 SRT 报告
  - 这比继续增加更多假阶段字段，更接近真实可演示产品闭环

### 决策 20：把导出渲染做成纯函数模块，再由 API 包装成下载响应

- 决策内容：
  - 新增 `domain/jobs/exports.py`
  - 由纯函数负责把结构化结果渲染成 TXT、Markdown、JSON、SRT、WebVTT
  - 路由只负责状态检查和 HTTP 响应头
- 原因：
  - 避免把格式拼接散落在路由层
  - 后续前端下载、批量导出、CLI 工具或异步导出都能复用同一套逻辑

### 问题 18：Windows 下 Celery worker 消费任务后崩溃，任务表面上停留在 `queued`

- 表现：
  - API 仍能成功创建任务
  - Redis 队列能收到任务
  - 但 worker 在处理时因为 `asyncpg` 与 Windows 默认 Proactor event loop 组合报错，导致任务无法正常完成
- 解决方法：
  - 新增 `worker/runtime.py`
  - 在 worker 启动和 task 执行前统一调用 `configure_worker_event_loop()`
  - 在 Windows 下切换到 `WindowsSelectorEventLoopPolicy`

### 问题 19：后台自动化场景里，`conda run` 启动 worker 不够稳定

- 表现：
  - 在本地自动化脚本里，用 `conda run ... python -m ...` 后台起 worker 时，进程状态不够直观，排障成本高
- 解决方法：
  - 自动化联调时直接使用 `C:\Users\12917\.conda\envs\singlish-agent\python.exe -m singlish_agent_api.worker.run_worker`
  - 对用户日常手动运行而言，仍然推荐先激活 `singlish-agent` 再执行 `python -m ...`

### 联调记录：导出链路已真实跑通

- 验证方式：
  - 生成本地 wav
  - 上传任务并等待 worker 完成
  - 调用 `/exports/md` 与 `/exports/srt`
- 验证结果：
  - job_id：`45b14e14-1c1a-464a-8313-21cbaf3206b2`
  - `status=completed`
  - `provider=faster_whisper`
  - Markdown 导出成功，返回 `Content-Disposition`
  - SRT 导出成功，首条字幕为：
    - `00:00:00,000 --> 00:00:02,000`
    - `This cue is quite fast.`
- 结论：
  - 当前离线 MVP 已具备“上传 -> 转写 -> 结构化结果 -> Markdown/SRT 导出”的真实闭环

## 2026-06-11 下一阶段 A 分段接口决策

### 决策 21：把 transcript segments 提升为独立 API，而不是只嵌在 job detail 里

- 决策内容：
  - 新增 `/api/v1/jobs/{job_id}/segments`
  - 返回 `job_id`、`status`、`total_segments` 和分页后的 `segments`
- 原因：
  - 这更接近 `project-plan.md` 里对时间轴和分页片段接口的设计
  - 后续就算 segments 独立入库，前端也不用重写交互层

### 决策 22：前端通过新接口消费时间轴，而不是直接从 `result_payload` 硬读

- 决策内容：
  - 新增 `JobSegmentList` 组件
  - 页面轮询时同时请求 job detail 和 job segments
- 原因：
  - 让前端更早适应“详情接口”和“片段接口”分离的真实形态
  - 减少后面从 demo 数据迁到真实存储结构时的改动范围

### 问题 20：Windows 下 worker 不只是 Proactor loop 问题，还存在跨事件循环复用连接的问题

- 表现：
  - 即使切到 Selector event loop，worker 在第二轮联调里仍会报：
    - `Future attached to a different loop`
  - 任务表面看起来会停在 `queued`
- 解决方法：
  - 不再对每个 Celery task 使用 `asyncio.run()`
  - 改为让 worker 进程复用同一个 asyncio event loop
  - 避免 `asyncpg` 连接和 SQLAlchemy async 会话跨 loop 复用

### 联调记录：segments 链路已真实跑通

- 验证方式：
  - 生成本地 wav
  - 上传任务并等待完成
  - 调用 `/api/v1/jobs/{job_id}/segments`
- 验证结果：
  - job_id：`33a20e81-4bdf-43b1-8cf2-eaf7c935e06c`
  - `status=completed`
  - `total_segments=1`
  - 首条 segment：
    - `start_seconds=0.0`
    - `end_seconds=2.0`
    - `text=This cue is quite fast.`
- 结论：
  - 当前离线链路已经能返回真实时间戳分段
  - 项目正在从“整段 transcript demo”往“可消费的时间轴数据”推进

## 2026-06-11 下一阶段 A 真实预处理决策

### 决策 23：把 preprocessing 从固定演示值升级为真实音频规范化结果

- 决策内容：
  - 新增 `infrastructure/audio/preprocess.py`
  - worker 在转写前先对下载后的音频做真实预处理
  - `result_payload.preprocessing` 不再写死，而是回填真实 duration、sample rate、channels、normalized format
- 原因：
  - 第一阶段不仅要“能转写”，也要能证明离线链路里确实存在预处理这一步
  - 这比继续在结果层堆字段更贴近真实产品流水线

### 决策 24：预处理模块单独抽象，不把转码逻辑塞回 worker 主流程

- 决策内容：
  - 新增独立的 `preprocess_audio_file()` 异步入口
  - 由它返回规范化后的临时音频路径和元信息
  - worker 只消费预处理结果
- 原因：
  - 让预处理、转写、标准化的边界更清楚
  - 后续如果要加 VAD、静音裁剪、哈希或缓存，这个模块更容易扩展

### 决策 25：使用 PyAV 做真实音频重采样和单声道规范化

- 决策内容：
  - 将 worker 预处理依赖明确为 `av`
  - 将输入音频规范化为 `pcm_s16le`、16kHz、mono 的 wav 临时文件
- 原因：
  - 这和当前 faster-whisper 离线链路衔接自然
  - 也能让 `preprocessing` 输出与后续转写输入真正一致

### 联调记录：真实 preprocessing 已跑通

- 验证方式：
  - 生成本地 wav
  - 上传任务并等待完成
  - 查看 job detail 的 `result_payload.preprocessing`
- 验证结果：
  - job_id：`963e1437-6876-48d4-918a-4026b6df88fa`
  - `status=completed`
  - `provider=faster_whisper`
  - `duration_seconds=2.548`
  - `sample_rate_hz=16000`
  - `channels=1`
  - `normalized_format=pcm_s16le`
  - transcript：`This cue is quite fast.`
- 结论：
  - 当前系统已经具备“上传 -> 真实预处理 -> 真实转写 -> 时间轴分段 -> 导出”的离线 MVP 主链路

## 2026-06-12 数据集骨架决策

### 决策 26：先补数据集与评测目录骨架，而不是先塞未经整理的音频

- 决策内容：
  - 新增 `data/README.md`
  - 新增 `data/eval/README.md`
  - 新增 `data/eval/manifest.example.jsonl`
  - 新增 `examples/README.md`
  - 新增 `docs/evaluation.md`
- 原因：
  - 用户已经开始关心“数据集在哪里”
  - 当前仓库没有正式数据集目录，会让后续评测、README 指标和样本管理持续混乱
  - 先把结构和规则定清楚，比仓促提交不明来源的音频更安全

### 决策 27：仓库先提交 manifest 与说明，不直接提交私有或不明授权音频

- 决策内容：
  - 用 JSONL manifest 示例描述未来评测集格式
  - 明确 `data/eval/` 用于本地私有评测集
  - 明确 `examples/` 只放可公开分发的短音频和期望结果
- 原因：
  - 这和 `project-plan.md` 中“许可允许的公开语料 + 经同意自录数据”的原则一致
  - 可以先推进工程化和评测格式，而不提前引入版权或隐私风险

### 决策 28：把本地评测集字段和标注流程继续细化到可直接执行

- 决策内容：
  - 新增 `data/eval/manifest.local.template.jsonl`
  - 新增 `data/eval/splits.local.template.json`
  - 新增 `data/eval/annotation-guide.md`
- 原因：
  - 用户已经明确选择“先定实际字段和标注流程”
  - 只有 README 级说明还不够，真正收集样本时仍然会反复纠结字段和操作顺序
  - 现在这样已经可以直接拿模板开始录入第一批 pilot clips

## 2026-06-12 下一阶段设计精简结论

### 决策 29：把后续离线增强拆成 4 个连续子项目

- 顺序：
  - `manifest.local` 初始生成脚本
  - 真实 VAD / 静音切分
  - Singlish -> Standard English -> 中文双版本结果
  - 低置信度片段重处理骨架
- 原因：
  - 这样每一步都能单独验收
  - 也能避免同时改动过多链路导致当前离线 MVP 回归

### 决策 30：继续沿用 `result_payload`，先不拆独立数据库表

- 决策内容：
  - VAD、双语结果、reprocess 状态先继续放在 `result_payload` 内
  - 维持现有 job detail / segments / export 接口形状
- 原因：
  - 现在最重要的是把离线链路做完整
  - 不是马上引入新的持久化复杂度

### 决策 31：中文版本先做工程骨架版，不先绑定真翻译模型

- 决策内容：
  - 先把 `standard_english` 和 `simplified_chinese` 两个结果字段、provider 边界和前端展示跑通
  - 允许先走规则优先和显式 fallback
- 原因：
  - 用户明确选择了 `A`
  - 先把结构跑通更稳，后续再替换真 provider

### 决策 32：VAD 和低置信度重处理第一版以“稳定可验证”优先

- 决策内容：
  - VAD 失败时回退整段转写
  - 低置信度重处理第一版先实现标记、入口和状态回写
- 原因：
  - 这能保证系统具备恢复路径
  - 但不会过早引入复杂的多轮纠错逻辑

## 2026-06-12 下一阶段实现记录

### 决策 33：manifest 生成脚本只产出“可人工补全骨架”，不自动猜测标签

- 决策内容：
  - 新增 `apps/api/src/singlish_agent_api/scripts/generate_eval_manifest.py`
  - 扫描本地音频目录，生成 `clip_id`、相对路径、时长和占位字段
  - 默认把 `scenario`、`speaker_id`、`source_type`、`consent_status` 等字段写为 `unknown`
- 原因：
  - 这和现有 `annotation-guide.md` 的人工标注流程一致
  - 可以马上降低本地评测集起步成本，但不会伪造标注质量

### 决策 34：manifest 单元测试改用真实 wav 样本，而不是假字节文件

- 决策内容：
  - 在 `apps/api/tests/unit/test_generate_eval_manifest.py` 里用标准库 `wave` 生成最小 wav
  - 同时校验 `duration_seconds` 和 JSONL 字段骨架
- 原因：
  - 脚本会通过 PyAV 读取真实音频时长
  - 如果测试只塞假字节，后续一旦接入真实时长探测就会变成脆弱测试

### 问题 21：当前终端默认 Python 不是 `singlish-agent` 环境，缺少 `av`

- 现象：
  - 直接运行 `python -m pytest` 时，解释器里没有 `av`
  - manifest 脚本和后续音频链路测试都不能依赖当前默认环境
- 解决方法：
  - 显式切换为 `C:\Users\12917\.conda\envs\singlish-agent\python.exe`
  - 后续后端测试统一优先用这个解释器执行，避免 base 环境与项目依赖漂移

### 决策 35：真实 VAD 第一版采用“能量阈值 + 静音桥接 + 全段回退”

- 决策内容：
  - 新增 `apps/api/src/singlish_agent_api/infrastructure/audio/vad.py`
  - 对规范化后的 mono wav 按帧计算能量，产出 `speech_segments` 与 `silence_segments`
  - 若 VAD 异常或整段没有打出语音片段，则自动回退为整段 speech segment
- 原因：
  - 不额外引入新模型依赖，能先把可验证的离线切分链路跑通
  - 回退策略能保证当前 MVP 主链路稳定，不会因为切分失败导致任务整体失败

### 决策 36：双语输出通过独立 normalization provider 接入，而不是继续写死在 worker 里

- 决策内容：
  - 新增 `apps/api/src/singlish_agent_api/infrastructure/normalization/provider.py`
  - worker 通过 provider 返回 `standard_english`、`simplified_chinese`、`translation_provider`
  - 导出与前端结果面板同步展示双语结果
- 原因：
  - 这样后续替换真实翻译 provider 时不需要再改 worker 主流程
  - 也让“规则回退”和“真实 provider”之间的边界更清晰

### 决策 37：低置信度重处理第一版同时在 segment 级和 payload 级保留状态

- 决策内容：
  - `transcription.segments[*]` 新增 `low_confidence`
  - `result_payload.reprocess` 新增 `low_confidence_segments`、`reprocess_status`、`reprocess_attempts`
  - 新增 `POST /api/v1/jobs/{job_id}/reprocess-low-confidence` 骨架接口
- 原因：
  - 前端时间轴需要逐段可见的标记
  - API 和导出层又需要一个聚合后的重处理状态块，便于后续扩展为真正的二次处理任务

### 问题 22：`audioop` 在 Python 3.13 将被移除，不适合作为新 VAD 的长期依赖

- 现象：
  - VAD 单元测试最初通过，但出现 `audioop` 的弃用告警
  - 如果后续 Python 版本升级，这一实现会变成潜在兼容性风险
- 解决方法：
  - 改为用标准库 `array` + `math` 自行计算 RMS
  - 保留当前无额外依赖的优点，同时移除未来版本升级隐患

### 问题 23：中文常量在部分源文件和测试文件中出现编码不稳定

- 现象：
  - 在 Windows 终端链路里，个别中文字符串出现乱码风险
  - 这会让源文件编辑、补丁匹配和测试断言都变得脆弱
- 解决方法：
  - 在代码与测试中把关键中文常量改为 Unicode 转义
  - 保证仓库文本在不同终端编码条件下仍可稳定编辑、运行和比对
